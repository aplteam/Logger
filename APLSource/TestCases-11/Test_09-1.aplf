 R←Test_09(stopFlag batchFlag);path;isUnicode;list;parmSpace;msg;myLogger;buffer;⎕TRAP
⍝ Different combinations of filename, filenamePrefix, filenamePostfix and filenameType.
 R←∆Failed
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 path←GetFileRoot,'LogFiles'
 isUnicode←80=⎕DR' '
 :If #.FilesAndDirs.IsDir path
     list←↑#.FilesAndDirs.Dir path,'/*.log'
     #.FilesAndDirs.DeleteFile list
 :EndIf

 parmSpace←##.Logger.CreatePropertySpace
 parmSpace.debug←1
 parmSpace.filenamePrefix←'PREFIX_'
 parmSpace.filenamePostfix←'_POSTFIX'
 parmSpace.path←GetFileRoot,'LogFiles'
 parmSpace.printToSession←1
 parmSpace.active←1
 parmSpace.filenameType←'MONTH'

 myLogger←⎕NEW ##.Logger(,⊂parmSpace)
 →FailsIf 0≠myLogger.errorCounter

 msg←'There is no problem'
 myLogger.Log msg

 →FailsIf 0≠myLogger.errorCounter

 buffer←#.APLTreeUtils.ReadUtf8File myLogger.FullFilename
 →FailsIf 3≠⍴buffer
 →FailsIf(#.FilesAndDirs.NormalizePath GetFileRoot,'LogFiles\PREFIX__',(⍕100⊥⎕TS[0 1]),'__POSTFIX.log')≢myLogger.FullFilename

 myLogger.Close
⍝ We set pre and post but no filename AND type is empty!
 list←↑#.FilesAndDirs.Dir path,'/*.log'
 #.FilesAndDirs.DeleteFile list

 parmSpace←##.Logger.CreatePropertySpace
 parmSpace.debug←1
 parmSpace.filenamePrefix←'PREFIX_'
 parmSpace.filenamePostfix←'_POSTFIX'
 parmSpace.path←GetFileRoot,'LogFiles'
 parmSpace.printToSession←1
 parmSpace.active←1
 parmSpace.filenameType←''

 myLogger←⎕NEW ##.Logger(,⊂parmSpace)
 →FailsIf 0≠myLogger.errorCounter

 msg←'There is no problem'
 myLogger.Log msg

 →FailsIf 0≠myLogger.errorCounter

 buffer←#.APLTreeUtils.ReadUtf8File myLogger.FullFilename
 →FailsIf 3≠⍴buffer
 →FailsIf(#.FilesAndDirs.NormalizePath GetFileRoot,'LogFiles/PREFIX____POSTFIX.log')≢myLogger.FullFilename

 list←↑#.FilesAndDirs.Dir path,'/*.log'
 #.FilesAndDirs.DeleteFile list

⍝ We set type to empty and do not define filename, post, pre: INVALID!!
 myLogger.Close
 parmSpace←##.Logger.CreatePropertySpace
 parmSpace.debug←1
 parmSpace.path←GetFileRoot,'LogFiles'
 parmSpace.printToSession←1
 parmSpace.active←1
 parmSpace.filenameType←''

 :Trap 11
     myLogger←⎕NEW ##.Logger(,⊂parmSpace)
     →FailsIf 1
 :Else
     :If 'No "filenameType" specified but no "filename", "filenamePostfix", "filenamePrefix" either'≢0⊃⎕DM
         →FailsIf 1
     :EndIf
 :EndTrap

⍝ to be on the save side:
 myLogger.Close
 list←↑#.FilesAndDirs.Dir path,'/*.log'
 #.FilesAndDirs.DeleteFile list

 R←∆OK
